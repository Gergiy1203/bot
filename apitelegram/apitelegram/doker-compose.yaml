version: '3'
services:
   bot:
     build:
       context: .
       dockerfile: Dockerfile
     ports:
       - "5000:5000"
       environment:
         - TELEGRAM_TOKEN=<your_telegram_token>
         - db
         - pytest
         - postgresqlserver
        - logger
        